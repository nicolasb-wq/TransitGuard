# ---------------------------------------------------------------------------
# TransitGuard — ignorierte Pfade
# ---------------------------------------------------------------------------

# Sandbox-lokaler Upload-Key (NICHT fuer Produktion — Dev-Maschine: eigener Key + Play App Signing)
android/key.properties
android/app/upload-keystore.jks

# .NET-Build-Artefakte
bin/
obj/
*.user

# Node/PWA
node_modules/
web/dist/
*.tsbuildinfo

# Flutter / Android-Build
app/build/
app/.dart_tool/
app/android/.gradle/
app/android/.kotlin/

# Lokale Laufzeit-/Log-Reste
*.log
.env

# QA-Gate-Rohausgaben: werden je Pass ueberschrieben (transiente Belege).
# Die verbindlichen Berichte sind docs/qa/qa-pass-N.md/.json — die bleiben getrackt.
docs/qa/logs/
