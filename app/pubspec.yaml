name: transitguard
description: ÖPNV-Companion Hamburg & Umland — Fahrplan, Live-Abfahrte, Störungen, Community-Kontrollhinweise.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  geolocator: ^13.0.1          # Standort-Autofill (Start-Haltestelle)
  shared_preferences: ^2.3.1   # Device-Token, Ticket-Bestätigung (24 h), Abo-Token — KEINE Konten

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
